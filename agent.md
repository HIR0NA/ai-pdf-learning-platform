# 🤖 คู่มือสำหรับ AI Agent (Agent Rules & Instructions)

ไฟล์นี้สร้างขึ้นเพื่อเป็น **System Prompt / คู่มือแนะนำ** ให้กับ AI Agents (เช่น Cursor, Windsurf, GitHub Copilot หรือผู้ช่วย AI อื่นๆ) ที่จะเข้ามามีส่วนร่วมในการเขียนหรือแก้ไขโค้ดในโปรเจกต์นี้ เพื่อให้ AI เข้าใจบริบท มาตรฐาน และข้อจำกัดต่างๆ ของ **AI PDF Learning Platform** ได้อย่างแม่นยำ

---

## 📌 1. ภาพรวมโปรเจกต์ (Project Overview)
- **ประเภทของโปรเจกต์:** แพลตฟอร์มเพื่อการศึกษา ขับเคลื่อนด้วย AI (Full-Stack Web Application)
- **Framework หลัก:** Next.js (App Router) + TypeScript
- **เป้าหมายหลัก:** ดึงข้อมูลและข้อความจากไฟล์ PDF และนำมาประมวลผลด้วย AI (Google Gemini) เพื่อสร้างสื่อการเรียนรู้อัตโนมัติ เช่น แบบทดสอบ (Quiz), แฟลชการ์ด, บทสรุปเนื้อหา, แผนภาพ (Mermaid) และตารางทบทวนบทเรียน

## 🛠️ 2. เทคโนโลยีที่ใช้งาน (Tech Stack)
เมื่อ AI ต้องเขียนโค้ด ให้ยึดหลักการใช้งานไลบรารีดังต่อไปนี้เป็นหลัก:
- **Frontend:** React 19, Next.js 16 (ระบบ App Router)
- **Styling:** CSS Modules หรือ Tailwind CSS (ตามโครงสร้างหน้าเดิมที่มีอยู่) ควบคู่กับไอคอนจาก Lucide React
- **Backend:** Next.js Route Handlers (สร้างไฟล์ `route.ts` ในโฟลเดอร์ย่อยของ `src/app/api/...`)
- **Database:** Prisma ORM เชื่อมกับฐานข้อมูล SQLite (ผ่านไฟล์ `dev.db`)
- **Authentication:** NextAuth.js (`next-auth`) สำหรับระบบบัญชี ล็อกอิน และจัดการสิทธิ์ผู้ใช้งาน
- **AI Integration:** Google Generative AI (`@google/generative-ai`) ควบคู่กับ Vercel AI SDK
- **Data Extraction & Visualization:** `pdf-parse` (ดึงข้อความจากเอกสาร), `Recharts` (สร้างกราฟ) และ `Mermaid.js` (สร้างแผนภาพความสัมพันธ์)

## 📏 3. กฎเกณฑ์การเขียนโค้ด (Coding Standards)

1. **ใช้ TypeScript อย่างเคร่งครัด:** 
   - ห้ามใช้ `any` หากไม่จำเป็นจริงๆ ให้สร้าง Interface หรือ Type Alias อย่างชัดเจนเสมอ
2. **ข้อกำหนดของ Next.js App Router:**
   - ใช้ **Server Components** เป็นค่าเริ่มต้นเพื่อประสิทธิภาพที่ดีที่สุด
   - หากต้องใช้ State (`useState`), Lifecycle (`useEffect`) หรือ Event Listeners (เช่น `onClick`) ให้ใส่ประกาศ `'use client'` ไว้ที่บรรทัดแรกสุดของไฟล์คอมโพเนนต์เสมอ
3. **การเข้าถึงฐานข้อมูล (Database Access):**
   - ห้ามเรียกใช้งาน Prisma Client จากฝั่ง Client Component โดยตรง ให้เรียกผ่าน Server Actions หรือ API Routes เท่านั้น
   - ต้องมีการจัดการ Error Handling (`try-catch`) เมื่อทำงานกับฐานข้อมูลทุกครั้งเพื่อป้องกันแอปแคช
4. **ความปลอดภัย (Security):**
   - ห้ามส่งข้อมูล API Keys หรือข้อมูลความลับที่สำคัญไปยังฝั่ง Client เด็ดขาด (ใช้ Environment variables ที่ไม่มีคำนำหน้า `NEXT_PUBLIC_` ในฝั่งเซิร์ฟเวอร์เท่านั้น)
   - ป้องกันการโจมตีประเภท XSS โดยต้องมีการ Sanitize Data เสมอเวลาเรนเดอร์ข้อมูลที่ได้รับจาก AI ลงบนหน้าเว็บ

## 🎯 4. รูปแบบพฤติกรรมและการตอบกลับของ AI (Agent Persona)
- **วิเคราะห์ก่อนตอบ:** เมื่อไม่แน่ใจในความต้องการของผู้ใช้งาน ให้ตั้งคำถามเพื่อความชัดเจน (Ask Clarifying Questions) แทนการเดาโครงสร้างเอาเอง
- **ใส่คำอธิบาย:** เมื่อมีการเขียนโค้ดใหม่หรือปรับแก้ลอจิกที่มีความซับซ้อน ให้เขียน Comments อธิบายกำกับไว้ในโค้ดเสมอ
- **สื่อสารเป็นภาษาไทย:** ให้ตอบกลับและอธิบายขั้นตอนการทำงานกับผู้ใช้งานเป็นภาษาไทยเป็นหลัก ให้ชัดเจน สุภาพ และกระชับ
- **ความปลอดภัยของระบบไฟล์:** ห้ามสร้างโฟลเดอร์นอกโครงสร้างโปรเจกต์เว้นแต่ได้รับคำสั่ง และหลีกเลี่ยงการลบไฟล์ที่สำคัญ (เช่น Prisma Schema หรือ Config ต่างๆ) โดยไม่ถามเพื่อความแน่ใจ

---
*คำแนะนำ: AI ทุกตัวที่ทำงานใน Repository นี้ กรุณาอ่านทำความเข้าใจและนำกฎเหล่านี้ไปประยุกต์ใช้กับการสร้างผลลัพธ์ทุกครั้ง เพื่อให้สอดคล้องกับมาตรฐานของโปรเจกต์*
