# 📂 โครงสร้างโฟลเดอร์ของโปรเจกต์ (Project Structure)

โปรเจกต์ **AI PDF Learning Platform** ได้รับการออกแบบตามมาตรฐานของ Next.js (App Router) ผสมผสานกับการจัดการฐานข้อมูลด้วย Prisma โดยมีรายละเอียดของโครงสร้างไฟล์และโฟลเดอร์ดังนี้:

## 📁 โครงสร้างหลัก (Root Directory)

- `src/` - โฟลเดอร์หลักที่เก็บ Source Code ทั้งหมดของแอปพลิเคชัน
- `prisma/` - โฟลเดอร์สำหรับจัดการฐานข้อมูล
  - `schema.prisma` - ไฟล์กำหนดโครงสร้างตารางฐานข้อมูลและโมเดลต่างๆ
  - `dev.db` - ไฟล์ฐานข้อมูล SQLite (ใช้งานในโหมดนักพัฒนา)
- `public/` - โฟลเดอร์เก็บไฟล์สื่อคงที่ (Static Assets) เช่น รูปภาพ ไอคอน และไฟล์อัปโหลด
- `.env` / `.env.example` - ไฟล์กำหนดตัวแปรสภาพแวดล้อม (Environment Variables) เช่น API Key และ URL ฐานข้อมูล
- `package.json` - ไฟล์กำหนดไลบรารีและคำสั่งสคริปต์ที่ใช้ในโปรเจกต์ (Dependencies & Scripts)
- `next.config.ts` - ไฟล์ตั้งค่าการทำงานของ Next.js
- `tsconfig.json` - ไฟล์ตั้งค่าการเขียนและการคอมไพล์ TypeScript

---

## 📁 เจาะลึกโฟลเดอร์ `src/` (Source Code)

### 1. `src/app/` (ระบบ Routing หน้าเว็บ)
ใช้ระบบ App Router ของ Next.js ซึ่งแต่ละโฟลเดอร์จะทำหน้าที่เป็น URL path ของเว็บไซต์
- `api/` - พื้นที่สำหรับสร้าง API Routes (Backend Endpoints)
- `dashboard/` - หน้าแดชบอร์ดหลักของผู้ใช้ (แสดงข้อมูล, อัปโหลด จัดการเอกสาร PDF)
- `about/`, `contact/` - หน้าข้อมูลเกี่ยวกับแพลตฟอร์มและติดต่อเรา
- `blog/` - หน้าระบบบทความและข่าวสาร
- `login/`, `register/` - หน้าสำหรับการเข้าสู่ระบบและสมัครสมาชิก
- `product/`, `checkout/` - หน้าระบบสินค้าและการชำระเงิน
- `layout.tsx` - โครงสร้างหลักของหน้าเว็บ (Global Layout) เช่น Navbar และ Footer ที่จะถูกนำไปใช้ในทุกหน้า
- `page.tsx` - หน้า Homepage ของแพลตฟอร์ม (หน้าแรก)

### 2. `src/components/` (ชิ้นส่วน UI หรือ Reusable Components)
ส่วนประกอบที่สามารถนำไปใช้งานซ้ำในหน้าต่างๆ ได้:
- `Navbar.tsx` / `Footer.tsx` - แถบนำทางและส่วนท้ายของเว็บไซต์
- `QuizApp.tsx` - คอมโพเนนต์สำหรับแสดงผลและเล่นแบบทดสอบ
- `FlashcardApp.tsx` - คอมโพเนนต์สำหรับแสดงและเรียนรู้ผ่านแฟลชการ์ด
- `StudySchedule.tsx` - คอมโพเนนต์แสดงผลตารางการเรียน
- `MermaidViewer.tsx` - คอมโพเนนต์สำหรับเรนเดอร์แผนภาพ Mermaid.js
- `Providers.tsx` - ใช้สำหรับห่อหุ้มแอปด้วย Context Providers (เช่น SessionProvider ของ NextAuth)

### 3. `src/lib/` (ฟังก์ชันช่วยเหลือและลอจิกส่วนกลาง)
- เก็บโค้ดที่สามารถเรียกใช้งานได้ทั่วโปรเจกต์ เช่น การตั้งค่า Prisma Client, การเชื่อมต่อ Google Gemini API (`@google/generative-ai`), และฟังก์ชันอำนวยความสะดวกอื่นๆ (เช่น `sanitize.ts` สำหรับกรองข้อมูล)

### 4. `src/context/` (การจัดการ State)
- โฟลเดอร์สำหรับจัดการ Global State ของแอปพลิเคชันผ่าน React Context API

### 5. `src/middleware.ts` (ระบบตรวจสอบและคัดกรอง)
- ทำหน้าที่ตรวจสอบ Request ทุกครั้งก่อนเข้าสู่หน้าเว็บ (เช่น ตรวจสอบ Authentication ว่าผู้ใช้ทำการล็อกอินแล้วหรือยัง หากยังไม่ล็อกอินจะไม่สามารถเข้าถึงโฟลเดอร์ `/dashboard` หรือ API บางส่วนได้ เป็นต้น)
